{
  "name": "waste-segregation-monitor",
  "version": "1.0.0",
  "description": "Waste Segregation Monitoring System for Urban Local Bodies",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run setup && npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
